<%# locals: () %>

<p class="hint">
  You can edit the URL to change the time period like <code>/5d</code> or <code>/2y</code>;
  known units are: <%= IntervalHelper::TIME_INTERVALS.map { |k,v| "<b>#{k}</b>#{v[1..].downcase}" }.join(", ").html_safe %>.
</p>
